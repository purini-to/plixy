apis:
  - name: "hello"
    proxy:
      path: "/"
      methods:
        - "GET"
      upstream:
        target: "http://localhost:9001"

  - name: "status"
    proxy:
      path: "/status"
      methods:
        - "GET"
      upstream:
        target: "http://localhost:9001"
